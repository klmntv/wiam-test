export const API_CONFIG = {
  BASE_URL: 'https://dummyjson.com',
} as const;

export const API_ENDPOINTS = {
  WORKPLACE_CATEGORIES: '/products/category-list',
  SUBMIT_APPLICATION: '/products/add',
} as const;

export const ROUTES = {
  HOME: '/',
  ADDRESS_WORK: '/address-work',
  LOAN_PARAMETERS: '/loan-parameters',
  SUCCESS: '/success',
} as const;
